//JS Donut Counter Lab

//declare 2 variables one for guests and one for donuts
//use prompt method to bring up a dialog box in the browser for the user to insert the number of guests and donuts.
//use parseInt to convert the users answer from a string to an integer.
var guests = parseInt(prompt("Please enter the number of guests", "Type Name Here");
var donuts = parseInt(prompt("Enter number of donuts available", "Type number of Donuts here");

  if (guests <= donuts){
    console.warn("There are " + parseInt(guests) + " guests and " parseInt(donuts) " donuts.  Enough for everyone!");
  } else {
    console.warn("There are " + parseInt(guests) + " guests and " parseInt(donuts) " donuts.  There's not enough!  Every man for themselves!");
  }
}
//write a conditional to check if there are enough donuts

  //alert the user with a message telling them the numbers of donuts and guests, and if there are enough donuts for each guest.
